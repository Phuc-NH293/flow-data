name: github_commit_analytics
version: "1.0.0"
config-version: 2

profile: github_commit_analytics
model-paths: ["models"]
test-paths: ["tests"]
clean-targets: ["target", "dbt_packages"]

models:
  github_commit_analytics:
    staging:
      +schema: staging
      +materialized: view
    mart:
      +schema: mart
      +materialized: table
